name: Mirror XML to Cloudflare Pages

on:
  workflow_dispatch: {}
  push:
    branches: [ main ]
    paths:
      - 'docs/**'

concurrency:
  group: mirror-to-cf-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      deployments: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy /docs to Cloudflare Pages
        id: deploy
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          projectName: ${{ secrets.CF_PROJECT_NAME }}
          directory: docs
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}

      - name: Wait for CDN propagation
        run: sleep 20

      - name: Verify price.xml availability
        shell: bash
        env:
          URL: https://kaspi-stock-mirror.pages.dev/price.xml
        run: |
          set -euo pipefail
          echo "🔎 Checking $URL"
          for i in {1..10}; do
            echo "Attempt $i..."
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 "$URL" || true)
            if [[ "$STATUS" == "200" ]]; then
              echo "✅ price.xml available (HTTP $STATUS)"
              exit 0
            fi
            echo "Still not ready (HTTP $STATUS). Waiting..."
            sleep 10
          done
          echo "❌ price.xml not reachable after retries"
          exit 1

      - name: Done
        run: echo "✅ Cloudflare mirror deployed and verified successfully."
